{
  "name": "money-market-strategies",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.19.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace @mms/contracts && npm run build --workspace @mms/research-kernel && npm run build --workspace @mms/experiment-registry && npm run build --workspace @mms/research-runner && npm run build --workspace @mms/strategy-simulator",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "lint": "eslint packages vitest.config.ts",
    "test": "vitest run",
    "verify": "npm run lint && npm run build && npm run typecheck && npm test",
    "research:tw": "node scripts/runTwStrategyResearchStudy.mjs"
  },
  "devDependencies": {
    "@types/node": "22.20.1",
    "eslint": "10.7.0",
    "typescript": "6.0.3",
    "typescript-eslint": "8.65.0",
    "vitest": "4.1.10"
  }
}
