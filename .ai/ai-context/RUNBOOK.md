# Runbook

- Work only in `/Users/kelvin/VibeCoding-WorkSpace/MoneyMarketStrategies-agent`.
- Keep canonical `main` clean at
  `/Users/kelvin/VibeCoding-WorkSpace/MoneyMarketStrategies`.
- The reusable worktree must be clean and detached between tasks.
- Use bounded task branches and non-force deletion after local fast-forward.
- Run `npm run verify` before integration and after restoring the detached tree.
- Do not add a remote, database, web/API framework, provider adapter, deployment,
  publication, legacy runtime, output, or artifact-path dependency without new
  owner authorization.
- The next task is the Research Evidence Kernel, not an ETF UI.
